# APM runtime state.
apm_modules/

# Local dogfood deployment of this repository's own primitives (`apm install` here).
# Portals commit their deployed tree; here the source is .apm/, so the copies are noise.
/.agents/
/.claude/skills/
/.claude/agents/
/.claude/rules/
/.codex/agents/
/.github/agents/
/.github/instructions/
/.github/copilot-instructions.md

# Lockfile of the dogfood install: no external dependencies, so nothing to reproduce. Portals commit theirs.
/apm.lock.yaml
