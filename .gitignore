# dotagents-managed local state — regenerated by `npx @sentry/dotagents --project install`.
# agents.lock and .agents/.gitignore are listed literally because `dotagents doctor` checks for them.
/.agents/
agents.lock
.agents/.gitignore

# Tool-native folders generated by dotagents (symlinks/configs into .agents/)
/.claude/skills
/.claude/agents
/.codex/
/.vscode/skills
