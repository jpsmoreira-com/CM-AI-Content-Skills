# Runtime overrides for dev container tasks.
# Copy to `.env` only for local experiments. Do not commit real secrets.

TFS_DASHBOARD_AUTH_MODE="Git Credentials"
REQUESTS_CA_BUNDLE="/etc/ssl/certs/ca-certificates.crt"

# Optional, only if PAT authentication is implemented for unattended runs.
# TFS_DASHBOARD_PAT=""
